@model Systemize.Models.ViewModel.Dashboard.DashboardEntire


@{
    ViewData["Title"] = "Dashboard";
}

<link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>



@if (Model.myAssigned != null)
{
    <div class="container mt-5">
        <div class="card">
            <div class="card-header" id="headingOne">
                <h5 class="mb-0">
                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                        My assigned <span class="badge badge-pill badge-primary">@Model.myAssigned.Count()</span>
                    </button>
                </h5>
            </div>

            <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordion">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Stage</th>
                                <th scope="col">Percent Complete</th>
                                <th scope="col">Metrics</th>
                                <th scope="col">Timeline</th>
                            </tr>
                        </thead>
                        <tbody>
                                            @foreach (var item in Model.myAssigned)
                                            {
                                                                    <tr>
                                                                        <th scope="row">@item.Id</th>
                                                                        <td><a href="/workflow/details/@item.Id"> @item.Name </a></td>
                                                                        <td>@item.CurrentStageName</td>
                                                                        <td>@item.PercentageComplete %</td>
                                    <td><a href="/Dashboard/Metrics/@item.Id"> Metrics </a></td>
                                    <td><a href="/Dashboard/Timeline/@item.Id"> Timeline </a></td>
                                                                    </tr>
                                                                 
                                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
   
}

<hr />


@if (Model.myDrafts != null && Model.myDrafts.Count() >= 0)
{
    <div class="container mt-5">
        <div class="card">
            <div class="card-header" id="headingOne">
                <h5 class="mb-0">
                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                        My drafts <span class="badge badge-pill badge-primary">@Model.myDrafts.Count()</span>
                    </button>
                </h5>
            </div>

            <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordion">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Stage</th>
                                <th scope="col">Percent Complete</th>
                                <th scope="col">Metrics</th>
                                <th scope="col">Timeline</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.myDrafts)
                            {
                                <tr>
                                    <th scope="row">@item.Id</th>
                                    <td><a href="/workflow/details/@item.Id"> @item.Name </a></td>
                                    <td>@item.CurrentStageName</td>
                                    <td>@item.PercentageComplete %</td>
                                    <td><a href="/Dashboard/Metrics/@item.Id"> Metrics </a></td>
                                    <td><a href="/Dashboard/Timeline/@item.Id"> Timeline </a></td>
                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

}

<hr />



@if (Model.myWatchList != null)
{
    <div class="container mt-5">
        <div class="card">
            <div class="card-header" id="headingOne">
                <h5 class="mb-0">
                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                        My watchlist <span class="badge badge-pill badge-primary">@Model.myWatchList.Count()</span>
                    </button>
                </h5>
            </div>

            <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordion">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Stage</th>
                                <th scope="col">Percent Complete</th>
                                <th scope="col">Metrics</th>
                                <th scope="col">Timeline</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.myWatchList)
                            {
                                <tr>
                                    <th scope="row">@item.Id</th>
                                    <td><a href="/workflow/details/@item.Id"> @item.Name </a></td>
                                    <td>@item.CurrentStageName</td>
                                    <td>@item.PercentageComplete %</td>
                                    <td><a href="/Dashboard/Metrics/@item.Id"> Metrics </a></td>
                                    <td><a href="/Dashboard/Timeline/@item.Id"> Timeline </a></td>
                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

}



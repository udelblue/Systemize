@model Systemize.Models.ViewModel.WorkflowEntire;

@{

    

}
<div>
 

@if (Model.Actions != null)
{
       
        
        <div>

            @foreach (var act in Model.Actions)
            {

                var url = "../Action/" + Model.Workflow.Id.ToString() + "?act=" + act.ActionType  ;

                <a href="@url" class="btn @act.Button btn-sm float-end mx-2 " role="button"> @act.Name </a>

            }

        </div>
}

</div>
